
import { momentize } from 'frontend/helpers/momentize';
import { module, test } from 'qunit';

module('Unit | Helper | momentize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentize([42]);
  assert.ok(result);
});

