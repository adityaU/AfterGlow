
import { questionDashboardSettings } from 'frontend/helpers/question-dashboard-settings';
import { module, test } from 'qunit';

module('Unit | Helper | question dashboard settings');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionDashboardSettings([42]);
  assert.ok(result);
});

