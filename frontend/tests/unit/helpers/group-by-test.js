
import { groupBy } from 'frontend/helpers/group-by';
import { module, test } from 'qunit';

module('Unit | Helper | group by');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = groupBy([42]);
  assert.ok(result);
});

