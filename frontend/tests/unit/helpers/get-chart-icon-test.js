
import { getChartIcon } from 'frontend/helpers/get-chart-icon';
import { module, test } from 'qunit';

module('Unit | Helper | get chart icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getChartIcon([42]);
  assert.ok(result);
});

