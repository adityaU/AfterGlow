
import { localize } from 'frontend/helpers/localize';
import { module, test } from 'qunit';

module('Unit | Helper | localize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = localize([42]);
  assert.ok(result);
});

