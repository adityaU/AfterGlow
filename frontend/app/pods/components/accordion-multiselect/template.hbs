{{searchable-select
    content=showableOptions
    sortBy=labelProperty
    multiple=true
    selected=selection
    optionLabelKey=labelProperty
    prompt=label
    closeOnSelection=false
    on-change=(action (mut selection))}}