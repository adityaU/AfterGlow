{{searchable-select
    content=databases
    sortBy="name"
    prompt="Select a Database"
    selected=queryObject.database
    optionLabelKey="name"
    on-change=(action (mut queryObject.database))}}
